services:
  mongodb:
    image: mongo:latest
    container_name: bm-container
    ports:
      - "27017:27017"
    volumes:
      - mongodb-data:/data/db
    networks:
      - banksphere-network

  backend:
    build: ./backend
    backend:
  build: ./backend
  image: kiran626/valora-backend:latest
    container_name: bb-container
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    depends_on:
      - mongodb
    networks:
      - banksphere-network

  frontend:
    build: ./frontend
    backend:
  build: ./backend
  image: kiran626/valora-frontend:latest
    container_name: bf-container
    ports:
      - "8081:80"
    depends_on:
      - backend
    networks:
      - banksphere-network

volumes:
  mongodb-data:

networks:
  banksphere-network:
